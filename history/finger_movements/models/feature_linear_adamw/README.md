# Archived Feature + AdamW Linear Model

This was an early Phase 1 neural-training baseline built on the retired UEA conversion. It extracted summary EEG features and trained a linear output layer with AdamW.

The associated performance is invalid after discovery of deterministic adjacent-channel overlap in that conversion. The implementation remains only to document the discarded direction; it is not an active dependency or a valid benchmark.
