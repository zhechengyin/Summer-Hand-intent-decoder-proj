# Active Experiments

There is currently no active experiment.

The completed FingerMovements Phase 1b–1h scripts are preserved under
`history/finger_movements/experiments/`. Their metrics, predictions, model,
and frozen checkpoint are stored beside them in the same archive.

Do not copy an archived script back into this directory as an implicit new
experiment. Before adding code, register the new question, validation policy,
candidate representations, and phase name in `docs/STATUS.md`.
