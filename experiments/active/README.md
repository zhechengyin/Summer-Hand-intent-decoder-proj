# Active Experiments

There is no active experiment at the Phase 2b closeout boundary.

Completed Phase A2, Phase 2b, and corrected-data terminal-Logistic comparison
scripts are archived under:

```text
history/finger_movements/experiments/
```

Their complete evidence is under:

```text
history/finger_movements/results/
```

The promoted offline research model is maintained separately under
`models/finger_movements/cssd_lda/`. The next experiment should receive a new
phase only when its causal-filter validation protocol is agreed. It must use
official TRAIN-only cross-validation and must not tune from the previously
opened official TEST.
