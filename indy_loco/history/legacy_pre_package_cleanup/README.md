# Legacy pre-package-cleanup archive

This directory is a provenance archive created when the active repository was
reduced to twelve canonical session packages. Its runners, checkpoints,
generated conversion files, reports and descriptions are **non-authoritative**.

Do not choose deployment weights from this directory. The only active package
index is `../../../models/manifest.json`.
