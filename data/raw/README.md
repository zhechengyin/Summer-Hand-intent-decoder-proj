# Raw immutable data

This directory contains verified source recordings exactly as downloaded or with
aliases documented by a manifest. Never edit files here in place. Derived arrays
belong in `../processed/`; conversion scripts belong in `../processing/`.
