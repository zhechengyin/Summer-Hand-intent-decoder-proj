# Models

There is one active model:

```text
models/finger_movements/terminal_logistic/
```

It contains the frozen causal low-pass, 252-feature extraction, normalization,
and Logistic Regression contract selected by Phase 1f. See its
[README](finger_movements/terminal_logistic/README.md).

Retired FingerMovements models are archived under
`history/finger_movements/models/`.
