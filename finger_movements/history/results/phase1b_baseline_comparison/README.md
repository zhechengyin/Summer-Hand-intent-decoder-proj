# Phase 1b Baseline Comparison — Invalid Source

These outputs compare Feature + Linear, Tiny MLP, Tiny EEGNet, and Tiny Multi-scale CNN on the retired UEA conversion.

The conversion was later found to contain deterministic adjacent-channel overlap. Consequently, all metrics in this directory are invalid for model comparison and must not be used to select the current model. The files are retained only to preserve the early experiment trail.
